# Rust build artifacts
/target/
**/target/
**/*.rs.bk
*.pdb

# Cargo lock is committed for binary crates (workspace contains binaries)
# Cargo.lock is included in version control

# Data directory (local only, never committed)
/data/

# Environment configuration
.env

# macOS
.DS_Store
.AppleDouble
.LSOverride
._*
.Spotlight-V100
.Trashes

# IDE and editor files
.idea/
.vscode/
*.swp
*.swo
*~
\#*\#
.#*

# Docker persistent volumes (if stored outside data/)
docker-data/
